interface ExpenseEntry {
    date: string;
    description: string;
    amount: number;
}

interface YnabExpenseEntry {
    id: string;
    date: string;
    description: string;
    amount: number;
}